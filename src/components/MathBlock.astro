---
import katex from "katex";

const { tex } = Astro.props;

const html = katex.renderToString(tex, {
  displayMode: true,
  throwOnError: false
});
---

<div class="my-8 text-center text-lg" set:html={html} />